// Configuration de l'application
(function() {
    // Clé API Google Maps/Places
    window.GOOGLE_API_KEY = 'AIzaSyCTJ-ttYO8KkKmvDGAFFpjRwiBJf9ciXrA';
    
    // Autres configurations globales de l'application peuvent être ajoutées ici
    
    // Note: Dans un environnement de production, ces clés API devraient être
    // gérées côté serveur et jamais exposées directement dans le code client.
    // Il est recommandé d'utiliser une solution plus sécurisée comme:
    // 1. Proxy backend pour masquer les clés API
    // 2. Restrictions de domaines dans la console Google Cloud
    // 3. Quotas et limites d'utilisation dans la console Google Cloud
})(); 